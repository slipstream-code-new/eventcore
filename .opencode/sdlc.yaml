# SDLC Configuration
# Generated by /setup on 2025-12-31

mode: library

git:
  workflow: git-spice
  require_clean: true

github:
  project: 11
  owner: jwilger

board:
  statuses:
    - Backlog
    - Ready
    - In Progress
    - Review
    - Done

tdd:
  verbosity: brief
  bypass_patterns:
    # Documentation
    - "*.md"
    - "*.txt"
    - "*.rst"
    # Infrastructure
    - ".github/**"
    - "*.tf"
    - "*.tfvars"
    - "*.hcl"
    # Configuration
    - "Cargo.toml"
    - "package.json"
    - "pyproject.toml"
    - "*.yaml"
    - "*.yml"
    - "*.toml"
    - "*.json"
    # Build/tooling
    - "Makefile"
    - "Dockerfile"
    - "docker-compose*.yml"
