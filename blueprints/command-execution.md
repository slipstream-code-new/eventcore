---
name: command-execution
summary: Command pattern with pure apply/handle, automatic retry, and dynamic stream discovery.
---

# Command Execution

Orchestrates the business logic lifecycle: read state from event streams, validate preconditions, produce new events, and persist atomically with configurable retry on concurrency conflicts.

## Overview

Commands implement `CommandLogic` (pure domain logic) and `CommandStreams` (stream declarations). The `execute()` function wires these together with an `EventStore` backend and `RetryPolicy`, handling the full read-validate-write-retry cycle.

## Architecture

### Execution Flow

```
execute(store, command, policy)
  │
  ├── Loop (attempt 0..max_retries):
  │   ├── Read all declared streams
  │   ├── Reconstruct state via apply() fold
  │   ├── Optional: dynamic stream discovery via stream_resolver()
  │   ├── Call handle(state) → NewEvents or CommandError
  │   ├── Build StreamWrites with expected versions
  │   ├── append_events() → success or VersionConflict
  │   │
  │   ├── On success: return ExecutionResponse(attempts)
  │   ├── On VersionConflict + retries left: backoff, continue
  │   └── On permanent error: return Err immediately
  │
  └── Max retries exhausted: return ConcurrencyError
```

### Command Traits

**CommandLogic** — Pure domain logic:

- `apply(state, event) → State` — Left-fold for state reconstruction
- `handle(state) → Result<NewEvents, CommandError>` — Business rule validation and event production
- `stream_resolver()` — Optional dynamic stream discovery

**CommandStreams** — Infrastructure concern:

- `stream_declarations() → StreamDeclarations` — Which streams this command reads/writes
- Typically generated by `#[derive(Command)]`

### Dynamic Stream Discovery

Commands can discover streams at runtime via `StreamResolver`:

1. Declared streams are read and folded into initial state
2. `stream_resolver().discover_related_streams(&state)` returns additional stream IDs
3. Additional streams are read and folded into state
4. `handle()` runs with the complete state

Use case: a command that discovers related entities from initial state (e.g., customer ID found in order stream).

### Retry Policy

| Parameter          | Default                 | Description                             |
| ------------------ | ----------------------- | --------------------------------------- |
| `max_retries`      | 4 (5 total attempts)    | Maximum retry count on version conflict |
| `backoff_strategy` | Exponential (10ms base) | Fixed or exponential with ±20% jitter   |
| `metrics_hook`     | None                    | Callback for retry observability        |

Jitter formula: `delay * (1.0 + (random - 0.5) * 0.4)` — produces [0.8x, 1.2x] of base delay.

### Error Classification

| Error                        | Retryable | Description                           |
| ---------------------------- | --------- | ------------------------------------- |
| `BusinessRuleViolation`      | No        | Domain rule failed                    |
| `ConcurrencyError(attempts)` | Auto      | Version conflict, retried by executor |
| `EventStoreError`            | No        | Infrastructure failure                |
| `ValidationError`            | No        | Logic error in command setup          |

## Key Types

| Type                 | Purpose                                  |
| -------------------- | ---------------------------------------- |
| `RetryPolicy`        | Configures max retries, backoff, metrics |
| `BackoffStrategy`    | Fixed or Exponential with jitter         |
| `ExecutionResponse`  | Success result with attempt count        |
| `NewEvents<E>`       | Wrapped Vec of events from handle()      |
| `StreamDeclarations` | Validated collection of stream IDs       |
| `CommandError`       | Typed error enum for all failure modes   |

## Files

| File                             | Description                                                      |
| -------------------------------- | ---------------------------------------------------------------- |
| `eventcore/src/lib.rs`           | `execute()`, RetryPolicy, BackoffStrategy, require! macro        |
| `eventcore-types/src/command.rs` | CommandLogic, CommandStreams, StreamResolver, StreamDeclarations |
| `eventcore-types/src/errors.rs`  | CommandError enum                                                |

## Related Systems

- [event-sourcing](event-sourcing.md) — Underlying persistence model
- [macro-codegen](macro-codegen.md) — Generates CommandStreams implementations
- ADR-006: Command macro design
- ADR-008: Retry logic with exponential backoff
- ADR-009: Stream resolver design
- ADR-014: Queue-based stream discovery algorithm
