# Postgres configuration for local development
# Copy to .env and customize if needed

# Postgres version for testing (15, 16, 17, or 18)
POSTGRES_VERSION=17

# Postgres port (change if you have local postgres running)
POSTGRES_PORT=5432
